export const useAuthLayout = () => {
  const paper = {
    display: "flex",
    justifyContent: "center",
    flexDirection: "column",
    alignItems: "center",
  };

  return { paper };
};

export default useAuthLayout;
